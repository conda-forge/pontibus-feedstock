{% set python_min = "3.11" %}
{% set name = "pontibus" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/OpenFreeEnergy/pontibus/archive/refs/tags/v{{ version }}.tar.gz
  sha256: f28ed8ad63fb1d17ca687c29500c29c00650e089656285313ccf049ac6533db8

build:
  noarch: python
  script: SETUPTOOLS_SCM_PRETEND_VERSION={{ version }} {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
  run:
    - gufe >=1.5.0,<1.7
    - openfe >=1.5.0,<1.7
    - numpy <2.3
    - openmm !=8.1.0,>=8.0.0,<8.3
    - openmmtools >0.24
    - openff-interchange >=0.4.8
    - openff-nagl-base >=0.3.3
    - pydantic >=2.0
    - python >={{ python_min }}
    - rdkit !=2024.03.06

test:
  imports:
    - pontibus
  requires:
    - pip
    - pytest
    - python {{ python_min }}
  commands:
    - python -c "import pontibus; assert(pontibus.__version__ == '0.2.0')"
    - pytest -v --pyargs pontibus

about:
  home: https://github.com/OpenFreeEnergy/pontibus
  summary: 'pontibus: experimental FF validation Protocols'
  description: |
    Experimental OpenFE Protocols for Force Field Evaluation
  license: MIT
  license_family: MIT
  license_file: LICENSE
  doc_url: https://pontibus.readthedocs.io/
  dev_url: https://github.com/OpenFreeEnergy/pontibus

extra:
  recipe-maintainers:
    - IAlibay
